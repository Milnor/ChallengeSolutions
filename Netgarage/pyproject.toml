[project]
name = "netgarage"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "paramiko>=3.5.1",
]

[dependency-groups]
dev = [
    "ruff>=0.11.5",
]
